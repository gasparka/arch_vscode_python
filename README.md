Visual Studio Code docker image - based on Arch Linux.

Aims to provide PyCharm like experience out of the box, see the `Dockerfile` for installed extensions and `settings.json`.

Run:
```
docker-compose up
```